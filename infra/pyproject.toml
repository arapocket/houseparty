[project]
name = "houseparty-infra"
version = "0.1.0"
description = "The AWS setup for House Party, written with AWS CDK"
requires-python = ">=3.12"
dependencies = [
    "aws-cdk-lib>=2.200",
    "constructs>=10.4",
]

[tool.ruff]
line-length = 100
target-version = "py312"
